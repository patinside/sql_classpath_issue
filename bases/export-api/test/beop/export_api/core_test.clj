(ns beop.export-api.core-test
  (:require [clojure.test :as test :refer [deftest is]]
            [beop.resources.test-system :as test-system]
            [beop.export-api.core :as core]))

(defn prepare-mysql-fixture []
  (let []))

(deftest dummy-test
  (is (= 1 1)))
